# Default values for helm.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1
image:
  repository: "yuriikosyi"
  tag: "v1.0.15-fa8921a"
  os: linux
  arch: amd64
secret:
  name: "kbot"
  env:
    TELE_TOKEN: "token"
    openweather_api_key: "openweather"
securityContext:
  privileged: true
